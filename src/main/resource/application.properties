# Server Port
server.port=8080

# Enable Debugging (Optional)
debug=true

# MongoDB Connection URI (Direct hardcoded value)
spring.data.mongodb.uri=${MONGO_URI}


# JWT Secret Key
jwt.secret=${JWT_SECRET}
